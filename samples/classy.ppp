PPP::import "foo/bar"

class Foo

  ###
  Block motha fucking quotes
  ###
  def __construct($foo, $bar)
    @foo = $foo
    @say $foo, true

  private def say($msg, $silly)
    if $silly
      echo $msg
    else 
      echo ">>>>$msg<<<<"

$c = new Foo "hello", 123
